import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

/**
 * Modules are registered dynamically.
 * See https://championswimmer.in/vuex-module-decorators/pages/advanced/dynamic.html
 */
const store = new Vuex.Store({});

export default store;
